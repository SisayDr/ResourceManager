namespace ResourceManagerAPI;

public enum DbOperationResult { Deleted, NotFound, InUse, UnAuthorized }

public enum ReservationStatus { Pending, Confirmed, Canceled }

public enum ReservationMode { shared, exclusive }

